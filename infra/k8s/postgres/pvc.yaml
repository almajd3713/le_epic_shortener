apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: db-data
  namespace: shortener

spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

