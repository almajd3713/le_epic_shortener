apiVersion: 1

providers:
  - name: Shortener dashboards
    orgId: 1
    folder: Shortener
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards/json
